{
    "name": "pi-channels",
    "version": "0.1.0",
    "private": true,
    "description": "Channel-based messaging and multi-agent coordination for the pi coding agent",
    "workspaces": ["packages/*"],
    "scripts": {
        "build": "npm run build -w agent-channels",
        "test:channels": "npm test -w agent-channels",
        "test:swarm": "npx tsx --test packages/pi-swarm/extensions/swarm-socket/tests/core/*.test.ts packages/pi-swarm/extensions/swarm-socket/tests/integration/*.test.ts",
        "test": "npm run test:channels && npm run test:swarm"
    },
    "license": "MIT"
}
